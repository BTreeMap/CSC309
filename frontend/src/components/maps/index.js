/**
 * Map Components
 * 
 * Components for displaying and interacting with maps and locations.
 * Uses OpenStreetMap (Leaflet) for rendering and Nominatim for geocoding.
 */

export { default as EventMap } from './EventMap';
export { default as LocationInput } from './LocationInput';
